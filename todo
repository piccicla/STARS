add code in main.py to process multiple images; think about the folders organization for NDVI

FIX BUG WHEN PARALLEL PROCESSING IS ON

recalculate heralick images with 10% subset

try parallel computing for SKll

test with python 2.7

automate ndvi creation (add something to paths.json / main.ini and consider them in the code )

automate Haralick creation (change code to consider paths.json / main.ini )